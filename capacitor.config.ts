import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.toorganizado',
  appName: 'TôOrganizado',
  // Empacota o export estático gerado por `npm run build:native`.
  webDir: 'out',
};

export default config;
